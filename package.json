{
  "name": "counter-widget",
  "version": "0.0.0",
  "description": "Dev-only test tooling for the counter-widget (jest + jsdom). No runtime/production dependency; no bundler.",
  "private": true,
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^24.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "scripts": {
    "test": "jest"
  }
}
